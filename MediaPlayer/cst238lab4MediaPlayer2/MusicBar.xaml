<UserControl x:Class="MediaPlayer.MusicBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <WrapPanel HorizontalAlignment="Center">
                <Button Name="btnPrev" Content="Prev" Command="MediaCommands.PreviousTrack"/>
                <Button Name="btnPlayPause" Margin="5,0" Content="Play" Command="MediaCommands.Play"/>
                <Button Name="btnStop" Content="Stop" Command="MediaCommands.Stop"/>
                <Button Name="btnNext" Margin="5,0" Content="Next" Command="MediaCommands.NextTrack"/>
            </WrapPanel>
        </StackPanel>
    </Grid>
</UserControl>
